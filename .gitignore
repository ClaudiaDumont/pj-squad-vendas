

# Environment & Secrets (AIOX)
.env
.env.local
.env.*.local
*.key
*.pem

# Dependencies (AIOX)
node_modules/
node_modules

# Build & Logs (AIOX)
dist/
build/
*.log
logs/

# IDE & OS (AIOX)
.DS_Store
Thumbs.db
.idea/
*.swp

# AIOX Local (AIOX)
.aiox-core/local/
.claude/settings.local.json
.aiox/install-log.txt

# AIOX runtime handoff artifacts (rule: .claude/rules/agent-handoff.md)
.aiox/handoffs/

# AIOX squad runtime state (session-context, pipeline state — nunca versionar)
.aiox/squad-runtime/

# Pipeline runtime outputs (.state.yaml etc.)
# Entregáveis persistentes (ex.: outputs/minds/) devem ser des-ignorados
# explicitamente aqui quando existirem.
outputs/

# Crash dumps (Git Bash no Windows)
*.stackdump
